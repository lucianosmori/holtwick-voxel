{
    "updated_at":  "2026-05-17",
    "iteration":  36,
    "started_at":  "2026-05-12T00:00:00Z",
    "experiment":  "holtwick-voxel",
    "consecutive_no_progress":  0,
    "last_error":  null,
    "status":  "exploring",
    "last_outcome":  "P7.1 done — `src/world/buildings.ts` stamps three new structures into the village grid: blacksmith forge (6×6 plank ring at (18,20) with east-side doorway at z=22 + single `VOXEL_STONE` anvil at interior centre + plank floor), village well (radius-2 stone ring at y=1 around (46,30) + `VOXEL_WATER` at centre y=0), and 2 market stalls at (20,36) + (24,36) (4 plank corner posts y=1..2 + 3×2 plank canopy at y=3, open-sided). `village.ts` calls `addBuildings(grid)` after `addTavern` and bumps `VILLAGE_HEIGHT` from 3 to 4 (= `1 + Math.max(TAVERN_WALL_HEIGHT, BUILDINGS_MAX_Y=3)`) so the y=3 canopy fits. `entities/player.ts` `hitsWall` now restricts its column scan to the player's torso y range (`yLo = floor(PLAYER_Y - half)`, `yHi = floor(PLAYER_Y + half)`) so the overhead canopy doesn't wall off the cells beneath the stall — keeps existing wall behaviour at y=1..2 unchanged. Build green 509.74 kB (+1.31 kB vs iter 35). Visual gate runs in CI on push. Next: P7.2 (5 more tavern NPCs spawned at new buildings)."
}
