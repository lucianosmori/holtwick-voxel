{
    "updated_at":  "2026-05-17",
    "iteration":  47,
    "started_at":  "2026-05-12T00:00:00Z",
    "experiment":  "holtwick-voxel",
    "consecutive_no_progress":  0,
    "last_error":  null,
    "status":  "exploring",
    "last_outcome":  "P8.4 animated water + chimney smoke shipped — `src/world/waterAnim.ts` `WaterAnimator(worldMesh)` finds the `voxel:5` InstancedMesh, snapshots baseY into a Float32Array on construction, and writes `baseY + sin((t+i*0.3)*1.5)*0.05` per instance every frame with `instanceMatrix.needsUpdate=true`. `src/render/particles.ts` `SmokeEmitter` builds a 64×64 radial-gray CanvasTexture shared across 16 `THREE.Sprite`s (`transparent: true`, `depthWrite: false`); each sprite has a fixed radial drift angle and a staggered phase offset `i*(4/16)` so the trail is continuous at t=0. `update(t)` recycles on a 4s cycle: `f = age/4`, position `(base + driftXZ*f, base.y + 2.5*f)`, scale `0.5+f*1.0`, opacity `0.7*(1-f)`. `src/main.ts` mounts the emitter at world `(gridOffset.x+32.5, 5, gridOffset.z+17.5)` (tavern roof centre per spec), binds `WaterAnimator(worldMesh)`, ticks both from RAF after worldItems. Test hooks `getSmokeSpritePositions()` (16 entries x/y/z/opacity) + `getWaterInstanceYs()`. `validate-visual.mjs` P8.4 block snaps both, waits 1000ms, snaps again, asserts ≥1 water Y delta + ≥3 smoke position deltas, captures `iter-${ITER}-smoke.png`. Build green 534.27 kB (+9.69 kB vs iter 46's 524.58 kB). Next: P8.5 (tavern sign + lamp posts)."
}
