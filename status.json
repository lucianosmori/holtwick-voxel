{
    "updated_at":  "2026-05-17",
    "iteration":  50,
    "started_at":  "2026-05-12T00:00:00Z",
    "experiment":  "holtwick-voxel",
    "consecutive_no_progress":  0,
    "last_error":  null,
    "status":  "exploring",
    "last_outcome":  "P8.7 3 new quests shipped — deliver-bread + find-the-spring + talk-to-all. `src/data/quest.schema.ts` extends `QuestTrigger` with 3 new variants: `{type:\"deliver\", item_id, npc_id}`, `{type:\"walk_to\", cell:{x,z}, radius}`, `{type:\"talk_to_all\", npc_ids: string[]}` (plan only specified the first two; talk_to_all needs its own type since the existing `talk_to` is single-target). `src/data/quests.ts` appends `edda_deliver_bread` (Edda → Petra the baker, deliver 1 bread, +15 gold — Edda's 2nd quest surfaces only after `edda_find_aldric` completes since `renderQuestRow` picks the first not_started), `hilda_find_spring` (Hilda → walk_to cell (8,60) radius 2 — SW village edge past the pond, +30 gold), `dorin_talk_to_all` (plan called for \"Bren\" — no such NPC; Dorin the plaza-wandering miner takes the social-butterfly quest; trigger lists all 12 NPC IDs including the giver, +50 gold). `src/game/inventory.ts` gains `consumeItem(id, count)` that decrements safely + emits a negative-delta pickup event so HUD/modal/auto-save react. `src/game/quests.ts` adds module-level `talkedTo: Map<questId, Set<npcId>>` (in-memory only — not persisted in SaveV1 to keep the schema additive-stable; `restoreQuestsState` re-seeds in_progress talk_to_all quests with the giver since the player has definitionally already met them at accept-time). `acceptQuest` seeds the talked-to set with `def.giver_npc_id` on accept. `onTalkTo` handles deliver path (consumes 1 item + completes when target NPC opens dialog) and talk_to_all path (adds npc to set, emits, completes once set covers all `npc_ids`). New `checkWalkTo(worldX, worldZ, gridOffset)` invoked per-frame from main.ts RAF loop after `checkPickups` — converts grid cells to world coords using the supplied offset and completes any in_progress walk_to whose target circle the player has entered. New `getTalkedToCount(id)` test hook. `src/main.ts` adds `getTalkedToCount` + `triggerOnTalkTo` to `__voxelTest__` (the latter shortcuts the talk-to-all gate from \"open 11 dialogs sequentially\" to \"call onTalkTo directly\" — much faster CI). `scripts/validate-visual.mjs` P8.7 block accepts each of the 3 quests in sequence, satisfies each via its dedicated path: deliver — addItem(bread,1) + openDialog(petra) → asserts consumeItem fired (bread before-1) + gold delta +15; spring — movePlayerTo(-23.5, 28.5) (cell (8,60) → world (gridOffset.x+8.5, gridOffset.z+60.5) with gridOffset=(-32,-32)) + poll for complete + assert gold delta +30 + capture `iter-${ITER}-spring.png`; talk-to-all — accept (set seeds with dorin) + 11× triggerOnTalkTo for remaining cast + assert complete + gold delta +50. Build green 539.07 kB (+2.09 kB vs iter 49's 536.98 kB)."
}
