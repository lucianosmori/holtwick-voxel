{
    "updated_at":  "2026-05-17",
    "iteration":  25,
    "started_at":  "2026-05-12T00:00:00Z",
    "experiment":  "holtwick-voxel",
    "consecutive_no_progress":  0,
    "last_error":  null,
    "status":  "exploring",
    "last_outcome":  "P6.3 done — item schema + 3 items + world spawn + pickup. `src/data/item.schema.ts` + `src/data/items.ts` ship the 3 spec items (gold_coin/health_potion/iron_ore with stack caps + heal effect). `src/game/inventory.ts` is a counts Map with subscribe/emit, `addItem` returns a `PickupEvent` whose `delta` is clamped by the per-item stack cap. `src/world/village.ts` `computeItemSpawns(seed, grid, npcCells, count=12)` walks a `mulberry32(seed ^ 0xa17e)` stream (separate from voxel layout stream so adding items doesn't shift world), rejects water/empty + any cell with a non-empty voxel at y≥1 (walls) + an 8×8 plaza-center buffer + cells within 1 voxel of an NPC, caps at 500 attempts. `src/entities/worldItem.ts` `WorldItem` wraps a 0.3³ emissive cube (gold uses metalness 0.7) with hover-bob `y = baseY + sin(t*2 + phase)*0.1` + slow yaw spin; phase derived deterministically from (worldX, worldZ). `main.ts` builds 12 items into `worldItems`, per-frame `checkPickups()` does 2D dist² vs `(PICKUP_RADIUS=0.55 + PLAYER_HALF=0.3)²`, on hit `addItem(id,1)` + flips `picked` + disposes mesh/geom/material + shows a debounced bottom-center `#pickup-toast`. `index.html` adds the toast div + CSS (rgba bg + amber border, fade/slide 0.25s, z-index 6) — placeholder until P8.2's queued slide-in replaces it. `__voxelTest__` extended: `movePlayerTo(x,z)`, `getInventory`, `getItemCount`, `getItemWorldPositions`, `addItem`. `validate-visual.mjs` post-quest gate: pull spawn list, warp player onto first un-picked item, poll `getItemCount(id) > 0`, assert inventory stack reflects pickup AND world entry flips `picked=true`, capture `iter-${ITER}-pickup.png`. Local `npm run build` green at 492.85 kB. Visual + dialog gates run in GitHub Actions (`.github/workflows/visual-validation.yml`) post-push. Next: P6.4 inventory modal triggered by `I` key."
}
