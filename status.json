{
    "updated_at":  "2026-05-17",
    "iteration":  44,
    "started_at":  "2026-05-12T00:00:00Z",
    "experiment":  "holtwick-voxel",
    "consecutive_no_progress":  0,
    "last_error":  null,
    "status":  "exploring",
    "last_outcome":  "P8.1 decorative props shipped — `src/world/props.ts` adds `computeProps(seed, grid, reserved, count=20)` + `buildPropMesh(props)`. Walks `mulberry32(seed ^ 0xb022)` over the 1-cell outer ring of 5 building zones (tavern footprint 8×6, blacksmith 6×6, well 5×5 around (46,30), 2 stalls 3×2) and keeps cells that are grass `VOXEL_FLOOR` at y=0 with an empty column at y≥1 — naturally skips dirt roads, plaza stone, pond water, plank floors, walls, foliage, well-ring stones, AND any cell reserved by NPC/item/tree spawns. Per-zone targets sum to exactly 20 (tavern 6 + blacksmith 6 + well 4 + stalls 2 each). Each prop is randomly barrel (scale 0.6×0.9×0.6, darker dirt texture as banded staves) or crate (0.7³, plank texture) — two `InstancedMesh` siblings sharing the 1×1×1 `BoxGeometry`, both shadow-cast/receive, positioned at y=1+halfHeight. `main.ts` reserves NPC + item + tree cells when calling `computeProps`, positions the mesh at the same gridOffset as the voxel mesh, exposes `__voxelTest__.getPropPositions()`, and updates the boot log to include `${props.length} props spawned`. `validate-visual.mjs` P8.1 block asserts 20 props total + adjacency to ≥2 of {tavern, blacksmith, well, stalls} via inflated-bbox cell tests, warps player near a tavern prop and captures `iter-${ITER}-props.png`. Build green 523.79 kB (+3.05 kB vs iter 43). Next: P8.2 (toast notification system — replace ad-hoc pickup text with styled slide-in stack)."
}
