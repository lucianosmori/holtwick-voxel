{
    "updated_at":  "2026-05-17",
    "iteration":  40,
    "started_at":  "2026-05-12T00:00:00Z",
    "experiment":  "holtwick-voxel",
    "consecutive_no_progress":  0,
    "last_error":  null,
    "status":  "exploring",
    "last_outcome":  "P7.5 done — proximity NPC idle barks shipped. `src/ui/npcBark.ts` `updateNpcBarks` runs per-frame: for each NPC inside an 8-voxel ring around player + NOT in dialog, schedules a bark at random 15-30s intervals (per-NPC state map), picks a random `def.barks_idle` string, spawns a `.npc-bark` div under `#npc-bark-layer`, each frame projects `mesh.position + (0,1.6,0)` through camera.project → canvas-rect XY, applies `translate(...) translate(-50%,-100%)` to anchor above the head. Lifecycle 200ms fade-in + 3500ms hold + 300ms fade-out = 4000ms total; projection.z>1 (behind camera) hides for the frame. Test mode (`setBarkTestMode(true)` when `?test=1`) drops interval to 150-400ms so the visual gate doesn't burn a 30s wall-clock wait. `dialog.ts` exports new `getCurrentDialogNpcId()`; main.ts passes it each frame so a talking NPC stops barking mid-conversation. `index.html` adds `#npc-bark-layer` (position:fixed inset:0, pointer-events:none, z-index 50) + `.npc-bark` styling (amber-bordered dark pill, 220px max-width, monospace 12px). `main.ts` destructures `canvas: gameCanvas` from `bootstrapScene`, builds `barkNpcs: BarkNpc[]` once from `interactables`, and `__voxelTest__` gains `forceBark(id)` / `getBarkCount()` / `getBarkTexts()` hooks. `validate-visual.mjs` P7.5 block warps player to `eddaPos + (0,1)`, calls `forceBark('edda')`, polls for `.npc-bark` count>=1 (timeout 3s), asserts text is one of Edda's 6 barks_idle entries, captures `iter-${ITER}-bark.png`. Build green 519.84 kB (+2.35 kB vs iter 39's 517.49 kB). Next: P7.6 (quest 3 — Visit the well, well-keeper as quest-giver, 3-cell trigger radius, +5 gold reward)."
}
