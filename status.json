{
    "updated_at":  "2026-05-17",
    "iteration":  33,
    "started_at":  "2026-05-12T00:00:00Z",
    "experiment":  "holtwick-voxel",
    "consecutive_no_progress":  0,
    "last_error":  null,
    "status":  "exploring",
    "last_outcome":  "P6.10 done — lantern night lighting. `src/render/lanterns.ts` `buildLanterns(gridOffset)` returns 4 warm-orange (0xffb070) `THREE.PointLight`s, range 6, max intensity 1.5, mounted at world y=3 over cells: tavern-doorway (one south of `TAVERN_DOORWAY_X`), plaza NE/NW/SE corners (derived from PLAZA_HALF=10 around village centre 32,32). `lanternIntensityForPhase(p)` reuses the sun's elevation curve (`cos(p*2π)`) shifted +0.3 so lanterns ignite at ~70% sun-fall (dusk pool reads before full dark), peaks at midnight, symmetric falloff through dawn — matched to dayNight's real convention (0=noon, 0.25=dusk, 0.5=midnight, 0.75=dawn). `main.ts` adds the 4 lights to the scene, calls `updateLanterns` once pre-RAF + every frame after `dayNight.update(dt)`. `__voxelTest__` extended with `setDayNightPhase(p)` + `getLanternIntensities()`. `validate-visual.mjs` P6.10 block drives noon → all dark, drives midnight → all ≥1.0, captures `iter-${ITER}-lanterns.png`, resets to noon. Build green 507.66 kB (+2.15 kB vs iter 32). Next: P6.11 FPS counter overlay (backtick toggle)."
}
