{
    "updated_at":  "2026-05-17",
    "iteration":  28,
    "started_at":  "2026-05-12T00:00:00Z",
    "experiment":  "holtwick-voxel",
    "consecutive_no_progress":  0,
    "last_error":  null,
    "status":  "exploring",
    "last_outcome":  "P6.5.1 done — save/load double-pickup regression fix. Picked world-item indices now persist as `SaveV1.picked_item_indices: number[]` (additive on v1; older saves default []). `inventory.ts` adds `pickedIndices: Set<number>` + `markPicked/getPickedIndices/isPicked/restorePickedIndices/subscribePicked`. `save.ts` snapshot writes via `getPickedIndices`, `applySave` calls `restorePickedIndices` AFTER inventory restore + BEFORE main.ts spawns world items; `bindAutoSave` also subscribes to picked events defensively. `main.ts` switched `worldItems: (WorldItem | null)[]` parallel-indexed with `itemSpawns` — load loop pushes null for `isPicked(i)`, pickup loop `markPicked(i)` + nulls slot + disposes mesh, RAF + `getItemWorldPositions` filter nulls. `validate-visual.mjs` sub-check: pre/post-reload world-item count must match (regression would respawn picked slot + re-auto-collect, doubling stack — exact symptom from CI run 25983800585 commit 42be396 `health_potion 1→2`). Build green 497.71 kB. Visual + dialog gates run in GitHub Actions on push. Next: P6.6 second quest — Finn collect 3 iron ore."
}
