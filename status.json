{
    "updated_at":  "2026-05-17",
    "iteration":  56,
    "started_at":  "2026-05-12T00:00:00Z",
    "experiment":  "holtwick-voxel",
    "consecutive_no_progress":  0,
    "last_error":  null,
    "status":  "exploring",
    "last_outcome":  "P9.5 stars at night — `src/render/sky.ts` now exports `ProceduralSky` ({ texture, setNightAlpha, getNightAlpha }). 80 mulberry32(seed=42) stars on the +Y top face per spec; an additional 24 mulberry32(seed=43) stars are painted on the top-fifth zenith band of each horizon face because the camera (locked ~35° below horizontal, FOV 55°) never samples the +Y face on-screen, so the visible night sky needs paint on horizon faces. setNightAlpha early-returns when alpha hasn't changed beyond 1e-3, so calling it every frame costs one comparison except at the two phase crossings (0.7 in, 0.95 out) per 120s cycle. `src/render/scene.ts` returns `sky` in the SceneBundle. `src/main.ts` adds `nightAlphaForPhase(phase)` = 1 when phase ∈ [0.7, 0.95] else 0, calls `sky.setNightAlpha(...)` on boot, each frame, and inside the `?test=1` setDayNightPhase hook. New `getSkyNightAlpha()` test hook. `scripts/validate-visual.mjs` adds a P9.5 block that flips phase to 0.25 (asserts alpha=0), then to 0.85 (asserts alpha=1), and captures iter-${ITER}-stars.png. Build green 547.36 kB (+1.09 kB vs iter 55's 546.27 kB)."
}
