{
    "updated_at":  "2026-05-17",
    "iteration":  34,
    "started_at":  "2026-05-12T00:00:00Z",
    "experiment":  "holtwick-voxel",
    "consecutive_no_progress":  0,
    "last_error":  null,
    "status":  "exploring",
    "last_outcome":  "P6.11 done — FPS overlay (backtick toggle). `index.html` adds `#fps-overlay` (bottom-right, monospace, hidden by default, `.show` flips display, `pointer-events:none`). `src/ui/hud.ts` exports `setFpsOverlayText/setFpsOverlayVisible/isFpsOverlayVisible`. `src/main.ts` runs a rolling 60-frame `Float32Array` of `dt` values + a `fpsRenderCounter`; every 10 frames (when visible) writes `${fps}fps · ${draw} draw · ${ms}ms` with `fps = round(1/avgDt)`, `draw = renderer.info.render.calls` (snapshotted post-render), `ms = (avgDt*1000).toFixed(1)`. Backtick keydown (`e.code === \"Backquote\"`) toggles visibility, gated by local `isEditableTarget` + `e.repeat` guard so holding doesn't strobe. Sampler runs every frame even while hidden so window stays warm. `scripts/validate-visual.mjs` P6.11 block asserts hidden at boot, presses Backquote, waits ≤2s for `#fps-overlay.show` AND text matching `^(\\d+)fps` with N>0, presses Backquote again to confirm toggle-off. Build green 508.43 kB (+0.77 kB vs iter 33). Next: P6.12 README v1 + showcase screenshot capture script."
}
