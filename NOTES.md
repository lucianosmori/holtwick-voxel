# NOTES.md

Running journal for this experiment. One line per iteration. Free-form entries above the iteration log for discoveries, dead-ends, decisions.

## Discoveries & decisions

<!-- Write here when something surprises you or when you change direction. Dated. Kept forever — this is the post-mortem source material. -->

- **2026-05-12 — iter 1 spec lock.** Confirmed `../02-fp-overnight-roguelike/src/` has the 5 lift targets intact: `data/npc.schema.ts`, `data/npcs.ts`, 31 JSONs under `data/npcs/`, `chat/webllm.ts`, `audio/sfx.ts`. Three.js version pinned to `^0.160` (latest stable in `02-fp` ecosystem; r160 is widely covered in WebGL2 examples). PixelLab integration deferred to P2 as a Node tool, not a runtime dep, to keep iter scope honest.
- **Stack rationale (KISS, official-first):** Three.js is first-party from mrdoob/three; Vite is the official build path for browser TS; PixelLab is the only third-party for sprites and is run offline. No React-three-fiber wrapper — would add learning surface + bundle weight without value at this scale.
- **Why P1.7 is a hard checkpoint:** Per backlog risk register, the user wants to eyeball the first playable before ralph burns into procedural + chat. GH Pages URL in hand is the signoff trigger.
- **2026-05-13 — iter 13 visual-gate gotcha.** Initial Playwright run passed with zero console errors but the saved screenshot was solid black. Root cause: `THREE.WebGLRenderer` defaults `preserveDrawingBuffer: false`; Chromium's compositor clears the WebGL drawing buffer right after compositing, so a screenshot taken outside the renderer's RAF tick gets a wiped canvas. Fixed by enabling `preserveDrawingBuffer: true` only when the page is loaded with `?test=1` (so prod keeps the perf-friendly default). `validate-visual.mjs` always navigates to `?test=1`. **Lesson:** a "no console errors" + "canvas present" + "screenshot saved" pass is NOT sufficient validation on its own — the visual diff trail is what catches the silent-blank regression. Worth adding pixel-content asserts in a follow-up.

- **2026-05-13 — iter 20 PixelLab credits exhausted.** P3.1 `tools/genSprites.mjs` is written, schema-validated against PixelLab's `/create-character-with-4-directions` (initial 422 on `negative_description`+`no_background` told us the endpoint rejects those fields — stripped to the minimal `description` + `image_size` body validated by `02-fp` legacy), and ready to fire. A real call for the `rat` NPC reached the credit gate (HTTP 402 `"Insufficient resources. Generations remaining: 0, Credits: 0.0"`), so the batch is blocked on credit replenishment, not code. Skip-if-exists default means the script is idempotent: re-running `npm run gen:sprites` post-replenish will only call the API for NPC IDs whose 4 PNGs are missing.

**Run instructions (P3.1 batch):**
1. Top up PixelLab credits at `https://www.pixellab.ai/` (account already holds `~/.pixellab-token`, no rebind needed).
2. From this folder: `npm run gen:sprites` — generates all 31 NPCs (`<id>_{south,west,north,east}.png` under `public/sprites/`), ~1.5s sleep between calls, ~50s wall time.
3. Optional flags: `--only <npc_id>` smoke test; `--max <n>` cap; `--force` overwrite existing; `--sleep <ms>` adjust rate-limit; `--dry-run` print prompts only.
4. Commit the resulting `public/sprites/*.png` (124 files) and append an iter-21 entry noting the batch.

- **2026-05-12 — iter 12 deploy decision.** Repo only has one GH Pages site, already wired by `.github/workflows/pages.yml` for `02-fp-overnight-roguelike`. Instead of inventing a second workflow / `gh-pages` branch (would fight for the same Pages source), extended the existing workflow to build both POCs and combine into one artifact: 02-fp at root, 07 under `/07-fp-overnight-roguelike-3d/`. Vite `base: "./"` already produces relative asset paths so the subdir mount works without further config. **Live URL (post-deploy):** `https://lucianosmori.github.io/poc-fiesta-2026-05/07-fp-overnight-roguelike-3d/` — verify in browser, then signoff for P2.

## Iteration log

<!-- Appended by the loop. Format: `iter N | <done|blocked|skipped> | <one-sentence why>` -->
iter 1 | done | sharpened PROMPT objective + replaced TBD plan with 4 P0 / 7 P1 / 7 P2 tasks
iter 2 | done | P0.1 Vite+TS+three@0.160 scaffold; `npm run build` green (tsc --noEmit + vite build, 296ms)
iter 3 | done | P0.2 lifted npc.schema.ts + npcs.ts + 31 JSONs verbatim; added tsx-based `npm run check:data` (31 ok); build still green
iter 4 | done | P0.3 lifted chat/webllm.ts + audio/sfx.ts verbatim from 02-fp; build + data check green; not yet wired into runtime per spec
iter 5 | done | P0.4 three.js bootstrap: render/scene.ts (Scene + perspective cam @55°pitch + WebGLRenderer + resize) + RAF loop + rotating ref cube; build green (454 kB)
iter 6 | done | P1.1 VoxelGrid (Uint8Array, get/set/dims, EMPTY/FLOOR/WALL consts); OOB get returns empty for collision safety; build green
iter 7 | done | P1.2 buildVoxelMesh: per-type InstancedMesh sized by exact non-empty counts, shared BoxGeometry, default palette (floor/wall MeshStandardMaterial); build green
iter 8 | done | P1.3 buildStarterRoom() builds 32×1×32 floor-fill with WALL perimeter; main.ts renders it centered at origin in place of the rotating cube; build green (458 kB)
iter 9 | done | P1.4 Player class (0.6³ cube, WASD+arrows, axis-separated AABB collision vs VOXEL_WALL across full footprint) wired into main.ts RAF with capped dt; build green (460 kB)
iter 10 | done | P1.5 camera follow: each frame copy player pos + CAMERA_OFFSET, lookAt(player); build green (460 kB)
iter 11 | done | P1.6 BillboardNpc (PlaneGeometry 1×1.5 + canvas-texture MeshBasicMaterial, Y-axis-only lookAt) spawned at cell (16,22); build green (461 kB)
iter 12 | checkpoint | P1.7 extended `.github/workflows/pages.yml` to build 07 alongside 02 and mount it at `/07-fp-overnight-roguelike-3d/`; local build green; SPEC-LOCK — loop must halt for user signoff at `https://lucianosmori.github.io/poc-fiesta-2026-05/07-fp-overnight-roguelike-3d/`
iter 13 | done | P2.0 Playwright validation gate online — `scripts/validate-visual.mjs` (vite preview + chromium headless + screenshot + console-error guard), `npm run validate:visual`, loop.ps1/.sh prompt + post-iter gate; first uncovered issue: WebGL `preserveDrawingBuffer` (now URL-`?test=1`-gated in scene.ts) — visual gate was silently passing on cleared-canvas blank screenshots before the fix. iter-13.png confirms room+player+NPC render.
iter 14 | done | P2.0b pixel-content assert added to validate-visual.mjs — downsamples canvas#game to 512px long edge, bins colors at 5-bit/channel via getImageData, fails on >99% single-bin. Current build reads 67.43% top-bin / 51 bins; blank-canvas regression would read 100% / 1 bin and trip the gate. iter-14.png artifact captured.
iter 15 | done | P2.1 Kenney mini-block textures wired into voxelMesh — palette entries carry `textureUrl?` loaded via Vite `?url`+`TextureLoader` (cached, NearestFilter+SRGB+Repeat), grass/stone/dirt/planks PNGs from `assets/voxel/PNG/{Green,Light,Dark,Orange}/`, water keeps flat 0x3a6da6 (pack has no blue). New voxel-type consts DIRT=3/PLANKS=4/WATER=5 added. iter-15.png shows textured grass floor + stone walls; top-bin 67.4%→41.8%, bins 51→152.
iter 16 | done | P2.2 lighting: HemisphereLight(0xbcd6ff/0x4a3a2a, 0.4) + DirectionalLight(0xffeecc, 1.2) at (-18,30,-18) targeting origin; PCFSoftShadowMap on with sun ortho-frustum ±22, 2048² map, bias -5e-4; voxel instanced meshes + player opt into cast+receive shadow (NPC plane skipped — flat-shadow ugliness). Visual gate: top-bin 41.8%→37.15%, bins 152→165; iter-16.png shows player shadow on floor + warm-cool gradient on stone walls.
iter 17 | done | P2.3 procedural cube-sky (sky.ts; six 256² canvases — gradient horizon faces with warm band, radial zenith dome top, flat ground bottom) replaces flat 0x111418 background. The plan called for `assets/voxel/skybox/` PNGs but they weren't staged; procedural fallback keeps the iter unblocked and the swap-in is one line. Visual gate: bins 165→220 (+55 from sky gradient), top-bin steady at 37.15%; iter-17.png shows warm horizon band above the green floor.
iter 18 | done | P2.4 procedural village floor — `world/village.ts` `buildVillage(seed)` returns 64×1×64 `VoxelGrid`: 20×20 central stone plaza (`VOXEL_STONE=6`, new const, palette reuses wall stone texture), 7-wide N/S+E/W dirt roads, 2-deep dirt ring framing the plaza, 6 seeded scatter dirt patches (mulberry32 PRNG), and a jittered-edge circular water pond at (12,50) r=8 that only overwrites grass. `rooms.ts` deleted; `main.ts` switched to `buildVillage(1337)`; NPC re-anchored to plaza-SE. Sun position + shadow frustum widened (-30,50,-30) / ±40 / far 140 to cover the 64×64 world. `validate-visual.mjs` swapped flaky `waitForSelector` for `waitForFunction(data-engine attr)` after the bigger world made the prior wait flake. Visual gate iter-18.png shows all four biomes, top-bin 15.96% / 216 bins, no console errors. Build green 474.69 kB. Asset-emission gotcha: Vite collapses two same-basename ?url imports (Orange/texture_04 + Light/texture_04) into one dist file — sidestepped by reusing the wall stone texture for STONE rather than introducing a second `texture_04.png`.
iter 20 | done-script-blocked-batch | P3.1 sprite generator `tools/genSprites.mjs` (Node ESM) + `npm run gen:sprites`: reads `src/data/npcs/*.json` directly (bypasses Vite-only `npcs.ts` import.meta.glob loader), builds a fantasy-RPG anchor prompt + name/role/bark-keyword visual descriptors per NPC, calls PixelLab `/create-character-with-4-directions` (one POST → 4 dir PNGs), writes `public/sprites/<id>_{south,west,north,east}.png`, skip-if-exists default, --only/--max/--force/--sleep/--dry-run flags. Auth resolution: PIXELLAB_API_KEY → PIXELLAB_TOKEN → ~/.pixellab-token (legacy). Smoke-tested twice: first call returned HTTP 422 telling us `/create-character-with-4-directions` rejects `negative_description` + `no_background` (stripped to the minimal `{description, image_size}` body that the legacy `02-fp` gen.py validates); second call hit HTTP 402 "Insufficient resources. Generations remaining: 0, Credits: 0.0" → blocked on PixelLab credit top-up, not code. Build green 475.57 kB, visual gate iter-20.png unchanged from iter 19 (top-bin 15.96% / 320 bins). Follow-up: P3.1b (run-once + commit PNGs) added to plan, blocked on credits.
iter 22 | done | P4.2 WebLLM streaming wired into dialog (lazy-load on Send, `isWebGpuUsable()` precheck + scripted-bark fallback, Edda persona via new `tavernCast.ts`, Playwright-asserted via `?test=1` window hook + `iter-${ITER}-dialog.png` capture)
iter 23 | done | P6.1 quest schema + first quest (Edda→Aldric→10 gold): `data/quest.schema.ts` types + `data/quests.ts` corpus (helpers `questById`, `questsGivenBy`) + `game/quests.ts` state machine (in-memory map, gold purse, subscribe/emit, `acceptQuest`/`completeQuest`/`onTalkTo`); `ui/dialog.ts` openDialog calls `onTalkTo(npc.id)` to auto-complete `talk_to` quests targeting this NPC + appends `[Quest complete: … +N gold]` lines and renders accept-quest row when this NPC has a `not_started` quest; `index.html` adds `#dialog-quest-row` between chat-messages and input-row with warm-amber styling; `main.ts` `__voxelTest__` extended with `openDialog(npcId?)`, `acceptQuest`, `getQuestState`, `getQuests`, `getGold` (no-arg `openDialog()` still defaults to NPC[0] = Edda so validate-visual + test-dialog stay backward compatible). validate-visual asserts Edda→accept→Aldric→complete + 10 gold and captures iter-${ITER}-quest.png. `npcSpawns.ts` deliberately untouched — quest associations derive from `QuestDef.giver_npc_id`. HUD/toast deferred to P6.2 (in-chat confirmation line carries the iter). All 3 gates green (build 489.19 kB, visual top-bin 20.90% / 340 bins, test:dialog all checks pass).
iter 24 | done | P6.2 HUD — `src/ui/hud.ts` `mountHud()` renders gold + quest log into a top-right 200px panel (opacity 0.85, pointer-events: none, monospace, dashed divider, status-tinted rows) and subscribes to `subscribeQuests` so transitions re-render without polling. Quests filtered to `status !== "not_started"` so the log only surfaces accepted/completed work. `index.html` adds `#hud` + CSS; `main.ts` calls `mountHud()` after `bindTitle()`. `validate-visual.mjs` post-quest assertion: visible HUD AND `Gold: 10` AND `Quests (1)` AND a `[done] Find Aldric` row. Inventory row deferred to P6.3. All 3 gates green (build 489.84 kB main, visual top-bin 18.64% / 347 bins, test:dialog all checks pass).
iter 25 | done | P6.3 items + pickup — `data/item.schema.ts` + `data/items.ts` (3 items: gold_coin/health_potion/iron_ore with spec colors + stack caps + heal effect), `game/inventory.ts` (counts Map + subscribe/emit, `addItem` returns `PickupEvent {item_id, delta, total}` with delta clamped by stack cap), `world/village.ts` `computeItemSpawns(seed, grid, npcCells, count=12)` walks a `mulberry32(seed ^ 0xa17e)` stream (separate from voxel layout stream so adding items doesn't shift world), rejects water/walls/empty + 8×8 plaza-center buffer + 1-cell NPC dodge, 500-attempt cap. `entities/worldItem.ts` 0.3³ emissive cube `MeshStandardMaterial` (gold metalness 0.7), hover-bob `y = baseY + sin(t*2 + phase)*0.1` + slow yaw spin, deterministic phase from (worldX, worldZ). `main.ts` builds 12 items into `worldItems`, per-frame `checkPickups()` is 2D dist² vs `(PICKUP_RADIUS=0.55 + PLAYER_HALF=0.3)² = 0.7225`, on hit `addItem(id,1)` + flips picked + disposes mesh/geom/material + debounced `#pickup-toast`. `index.html` adds toast div + CSS (rgba bg + amber border, fade/slide 0.25s). `__voxelTest__` extended: `movePlayerTo(x,z)`, `getInventory`, `getItemCount`, `getItemWorldPositions`, `addItem`. `validate-visual.mjs` post-quest: pulls spawn list, warps player onto first un-picked item, polls `getItemCount(id) > 0`, asserts inventory stack reflects pickup AND world entry flips picked=true, captures `iter-${ITER}-pickup.png`. Build green 492.85 kB (HUD-only diff from iter 24 was 489.84→492.85 = +3.0 kB for inventory + worldItem + spawn util + dialog hook wiring).
iter 27 | done | P6.5 save/load — `src/game/save.ts` `SaveV1 v1` under `holtwick-voxel:save:v1` (player XZ + dayNight phase + quests + inventory + gold + saved_at); `loadSave` drops on parse error / version mismatch; `applySave` calls `restoreQuestsState` (new in `game/quests.ts`, rehydrates map + emits HUD re-render) + `restoreInventory` (new in `game/inventory.ts`, discards unknown items / clamps to stack cap / emits delta=0 per stack); `dayNight.setPhase()` no-ops when `?dayNight=` URL override active; `bindAutoSave` subscribes to quests + inventory + 30s heartbeat with 500ms debounce; `bindDialog` onClose also calls `scheduleSave`. `main.ts` loads + applies between player ctor and world-item spawn, then `bindAutoSave` after UI binds; `__voxelTest__` extended with `flushSave/clearSave/getDayNightPhase`. `validate-visual.mjs` post-inventory: flush save, `page.reload`, re-wait for data-engine+hook, assert quest complete + gold 10 + picked stack survived + HUD matches, captures `iter-${ITER}-reload.png`, calls `clearSave()` to keep chromium profile clean. Build green 497.16 kB.
iter 28 | done | P6.5.1 save/load double-pickup regression fix — picked world-item indices now persist alongside inventory. `inventory.ts` adds `pickedIndices: Set<number>` + `markPicked/getPickedIndices/isPicked/restorePickedIndices/subscribePicked`; `save.ts` `SaveV1.picked_item_indices: number[]` (additive on v1; older saves default `[]`), captured in snapshot via `getPickedIndices`, restored in `applySave` AFTER inventory + BEFORE main.ts spawns world items; `bindAutoSave` also `subscribePicked(scheduleSave)` defensively. `main.ts` switched `worldItems: (WorldItem | null)[]` parallel to `itemSpawns` — load loop pushes null for `isPicked(i)`, pickup loop `markPicked(i)` + nulls slot + disposes mesh, RAF tick + `getItemWorldPositions` filter nulls. `validate-visual.mjs` sub-check: pre/post-reload world-item count must match (regression would respawn the picked slot + re-auto-collect, doubling the stack — exact symptom from CI run 25983800585 commit 42be396 `health_potion 1→2`). Build green 497.71 kB.
iter 19 | done | P2.5 tavern building — new `world/tavern.ts` `addTavern(grid, {originX, originZ, doorwayX})` stamps 8×6 outer footprint (6×4 interior) of `VOXEL_PLANKS` floor at y=0 + 2-tall plank wall ring at y=1..2 with a 1-cell doorway gap in the south wall. `village.ts`: `VILLAGE_HEIGHT = 1 + TAVERN_WALL_HEIGHT = 3`; constructor no longer fills the whole volume with `VOXEL_FLOOR` (upper layers must stay empty), explicit y=0 ground fill instead; tavern stamped last at (originX=28, originZ=14, doorwayX=32) so the north dirt road becomes a plank threshold into the door. `player.ts` collision widened: any non-empty voxel at y≥1 in the footprint blocks (legacy `VOXEL_WALL` at y=0 kept for back-compat) — player slides along walls and walks through the doorway. Visual gate iter-19.png shows the orange tavern north of the plaza with a visible doorway gap, top-bin 15.96% / 320 bins (+104 vs iter 18 from plank surfaces), no console errors. Build green 475.57 kB.
iter 29 | done | P6.6 second quest (Finn → collect 3 iron ore, +25 gold) — `data/quest.schema.ts` `trigger` widened from inline `{type:"talk_to",npc_id}` to a discriminated union adding `{type:"collect",item_id,count}` (renamed to exported `QuestTrigger`). `data/quests.ts` appends `finn_iron_ore` (giver `finn`, trigger collect 3 `iron_ore`, reward 25 gold; description has Finn-the-bard fetching on Boran's behalf since the existing FINN is the bard and BORAN the smith — lore-consistent rather than retconning Finn's role + barks). `game/quests.ts` imports `getItemCount` + `subscribeInventory` from `./inventory` (one-way dep, no cycle); `acceptQuest` now also fires `completeQuest` immediately if a collect quest's threshold was already met at accept-time (player picks up items first, accepts after); `onTalkTo(npcId)` extended to also auto-complete in_progress collect quests whose giver is `npcId` when inventory threshold met (defensive — if a player accepted before having items and somehow the subscriber missed); new `bindCollectAutoComplete()` (idempotent) subscribes to `subscribeInventory` and completes any in_progress collect quest whose total crossed `count`. `main.ts` imports + calls `bindCollectAutoComplete()` after `bindInventory()` (post applySave so restore's delta=0 emits don't double-process). `validate-visual.mjs` post-save/load: snapshot gold, `acceptQuest("finn_iron_ore")`, assert in_progress, `addItem("iron_ore", 3)`, assert quest auto-completed AND gold == before+25, open Finn dialog + capture `iter-${ITER}-collect.png`, Esc close. Build green 498.38 kB (+0.67 kB vs iter 28 from the schema/subscriber/test-hook code). Visual + dialog gates run in GitHub Actions on push.
iter 26 | done | P6.4 inventory modal — `src/ui/inventory.ts` `bindInventory()` builds 12 fixed slots (4x3 grid) into `#inventory-grid`, `I` keydown toggles open/close (gated by `isEditableTarget` + dialog-open guard so typing "i" into chat lands in the input), Escape closes inventory, backdrop click closes too; subscribes to `subscribeInventory` so live pickups refresh while the modal is open. `index.html` adds `#inventory-backdrop` modal + 4x3 grid CSS (56px slots: 32px color square + name label + count badge top-right; empty slots show a dashed border; z-index 110 so it layers above the dialog backdrop at 100 should both somehow stack — but the dialog-open guard prevents that path). `main.ts` calls `bindInventory()` after `bindDialog()` + extends `__voxelTest__` with `openInventory/closeInventory/isInventoryOpen` for any future scripts (validate-visual exercises the real KeyI dispatch instead). `validate-visual.mjs` post-pickup: presses KeyI, asserts 12 slots present + >=1 populated (pickup test seeded inventory), captures `iter-${ITER}-inventory.png`, Esc closes, then opens dialog, focuses chat-input, presses KeyI, asserts `#inventory-backdrop.show` is NOT set AND chat-input value === "i" (the gate test). Build green 494.72 kB.
iter 30 | done | P6.7 NPC pathing — 3 walkers (Edda tavern-interior↔doorway pause 3s, Finn tavern-corner↔Boran-forge pause 2s, Cassia plaza-east↔tavern-approach pause 4s; substituted Cassia for Bren which isn't in the cast). `src/entities/npcWalker.ts` `NpcWalker(mesh, waypoints, gridOffset)` runs pause→walk state machine at 0.4 voxels/sec, halts when player < 3 voxels away (squared-distance check), loops the waypoint array forever. `data/npcSpawns.ts` `NpcSpawn.path?: PathWaypoint[]` (optional, only the 3 walker spawns populate it). `main.ts` collects walkers while building interactables + ticks each per RAF with `player.mesh.position`; `__voxelTest__.getNpcPosition(id)` returns world {x,z} or null. `validate-visual.mjs` post-collect-quest: warps player to (-26,-26) (cell ~(6,6), well outside every waypoint's halt radius), snapshots before, waits 6.5s (> longest 4s pause + meaningful walk distance), snapshots after, asserts all 3 walkers moved ≥0.1 voxels; captures `iter-${ITER}-walk.png`. Build green 499.68 kB (+1.30 kB vs iter 29).

iter 31 | done | P6.8 procedural audio — `src/audio/ambient.ts` shared AudioContext + master GainNode + day/night chains (day: 2s pink-noise buffer via Paul Kellet economy filter → 800Hz lowpass + 110Hz sine rumble; night: 4kHz sine carrier AM-modulated by 8Hz LFO, baseline 0.018 ±0.018 → throbbing cricket). isNight(phase) = phase ∈ [0.7, 0.95) per spec; `updateAmbient(dt, phase)` drives each chains gain via exponential approach (τ = CROSSFADE_SEC/3) so the crossfade hits ~95% in 3s. Daytime chirps: 1200-2400Hz sine bursts every 5-15s when day chain dominant. Volume persists to `holtwick-voxel:audio:volume`; default 0.5; clamped + written by `setMasterVolume`. `startAmbient()` exposes `window.__audioCtx` for the visual gate. `src/audio/footsteps.ts` `maybeStep(x,z)` fires when squared distance from cursor ≥ 0.09 (0.3 units); 50ms white-noise buffer → 600Hz lowpass + ±10% playbackRate jitter through master. `resetFootstepCursor` snaps cursor on warps (save restore + test movePlayerTo) so phantom steps dont fire. `main.ts` one-shot pointerdown/keydown/touchstart listeners call `startAmbient()` (browsers block AudioContext without trusted gesture); per-frame `maybeStep` + `updateAmbient`. `index.html` adds `#hud-volume-row` slider inside `#hud` (pointer-events: auto since parent HUD is non-interactive); wired in main.ts (input event → setMasterVolume(value/100)). `validate-visual.mjs` P6.8 block: `page.mouse.click(20,20)` for guaranteed trusted gesture, waits up to 4s for `__audioCtx.state === "running"`, asserts `#hud-volume` is type=range. Build green 503.50 kB (+3.82 kB vs iter 30).

iter 32 | done | P6.9 trees + foliage — `src/world/foliage.ts` `computeFoliage(seed, grid, npcCells, itemCells, count=30)` walks a `mulberry32(villageSeed+7331)` stream, rejects unless ground is `VOXEL_FLOOR` (grass) AND column above empty, enforces Chebyshev 8-cell plaza buffer + 2-cell road buffer (5×5 scan for `VOXEL_DIRT`) + 3-cell NPC + 3-cell item buffer, 800-attempt cap. `buildFoliageMesh(trees)` returns a `THREE.Group` with two InstancedMeshes — trunk uses Kenney `Dark/texture_05.png` (same dirt swatch the dirt voxel uses) for 4 cubes per tree at world y=1.5..4.5; canopy uses flat `0x2f6b22` (darker than grass texture dominant bin so foliage adds new pixel-content bins) for 8 cubes per tree: 4 orthogonal cells per layer × 2 layers (y=3.5, 4.5) — the 3×3 plus-shape minus its centre. `main.ts` computes `trees` after `itemSpawns`, builds mesh, positions at `gridOffset` so tree cells align 1:1 with floor cells. Build green 505.51 kB (+2.0 kB vs iter 31).
iter 33 | done | P6.10 lanterns — 4 warm PointLights (tavern door + 3 plaza corners) ramped from dayNight.phase via elevation-shifted nightness curve
iter 34 | done | P6.11 FPS overlay — `#fps-overlay` bottom-right monospace pill (hidden by default), backtick toggles visibility (gated by isEditableTarget + e.repeat), main.ts rolls a 60-frame Float32Array window of dt values + every 10 frames writes `${fps}fps · ${draw} draw · ${ms}ms` using `renderer.info.render.calls` post-render. validate-visual asserts hidden→press Backquote→visible AND text matches `^(\d+)fps` with N>0→press again→hidden. Build green 508.43 kB (+0.77 kB vs iter 33).
iter 38 | done | P7.3 minimap — `src/ui/minimap.ts` `mountMinimap({x,z})` returns `{ update(frame) }`. 150×150 2D canvas; static layer (BG `#1a1f2c` + 7-wide N/S+E/W dirt roads + 20×20 stone plaza + tavern outline as amber stroke over translucent amber fill) pre-rendered once into an offscreen canvas at mount; per-update we `clearRect + drawImage(base)` and overlay dynamic dots — items 2×2 gold, lanterns 4×4 warm orange (only when `intensity > 0.5`), NPCs 3×3 cyan, player 4×4 yellow on top. World→cell conversion uses the gridOffset passed at mount so axes align with the voxel mesh; village 64 voxels × 2 px/voxel = 128 px → 11 px margin centres inside 150. `index.html` adds `#minimap` canvas top-left (16px inset, amber 1px border, opacity 0.9, `image-rendering: pixelated`, `pointer-events: none`, z-index 5 matching HUD). `main.ts` mounts after `gridOffset` is computed, calls a one-shot `tickMinimap()` so the very first paint has the static layer + player dot, then re-runs every 10 RAF frames via `MINIMAP_RENDER_EVERY=10`. Lit threshold 0.5 catches dusk+midnight intensities (lantern max 1.5). `validate-visual.mjs` adds P7.3 block: asserts `canvas#minimap` is 150×150 + not display:none, reads `getImageData` over the full canvas, asserts ≥4 unique 24-bit colors AND ≥1 yellowish pixel (r>220, g 180-235, b<120) so static AND dynamic layers both ran. Captures `iter-${ITER}-minimap.png`. Build green 515.07 kB (+2.01 kB vs iter 37's 513.06 kB).
iter 37 | done | P7.2 roster — 5 new tavern-cast NPCs added: KARSTEN (apprentice smith) at blacksmith interior (21,22) next to anvil, HILDA (well-keeper) at (46,28) 2 cells north of well centre, PETRA (baker) at market stall 1 (21,36) between posts, RONAN (messenger) at market stall 2 (25,36) between posts, DORIN (miner) at plaza centre (28,32). Lifted no IDs from the dungeon corpus (all 31 there are hostile mobs); authored as fresh NpcDefs in `tavernCast.ts` with persona barks cross-referencing existing NPCs (Karsten→Boran, Petra→Edda/Cassia, Ronan→Aldric, Dorin→Boran/Edda) so dialog feels like a real village. `TAVERN_CAST` now [EDDA, FINN, ALDRIC, MIREILLE, BORAN, WREN, CASSIA, KARSTEN, HILDA, PETRA, RONAN, DORIN]. `npcSpawns.ts` `NPC_SPAWNS` appended 5 entries with placeholder backgrounds. `main.ts` `__voxelTest__` extended with `getNpcCount()` returning `interactables.length`. `validate-visual.mjs` adds a post-FPS-overlay P7.2 block asserting `getNpcCount() === 12`. Item + foliage spawn generators already exclude NPC cells (1-cell + 3-cell buffers respectively); 500/800-attempt caps absorb the +5 exclusions without dropping counts. Build green 513.06 kB (+3.32 kB vs iter 36's 509.74).
iter 36 | done | P7.1 buildings — `src/world/buildings.ts` adds `addBlacksmith` (6×6 plank ring at (18,20) with east-side doorway at z=22 + single `VOXEL_STONE` anvil at interior centre + plank floor), `addWell` (radius-2 stone ring at y=1 around (46,30) using `Math.abs(hypot(dx,dz) - 2) ≤ 0.5` → ~12 stones, water at centre y=0), and `addMarketStall` ×2 at (20,36) and (24,36) (4 plank corner posts y=1..2 + 3×2 plank canopy at y=3, no walls). `village.ts` calls `addBuildings(grid)` after `addTavern` so building voxels overwrite plaza/road tiles where they overlap. `VILLAGE_HEIGHT` bumped to `1 + Math.max(TAVERN_WALL_HEIGHT, BUILDINGS_MAX_Y=3)` = 4 so the y=3 canopy fits the grid. `entities/player.ts` `hitsWall` now constrains the column scan to the player's actual y range (`yLo = floor(PLAYER_Y - half)`, `yHi = floor(PLAYER_Y + half)`) so the canopy at y=3 doesn't wall off the cells beneath — fixes the open-sided stall semantic without changing wall behaviour at y=1..2. Build green 509.74 kB (+1.31 kB vs iter 35).
iter 35 | done | P6.12 README v1 + showcase capture — `scripts/capture-showcase.mjs` boots vite preview on :4175 and captures 4 named PNGs (`showcase-village-day.png` at `?dayNight=0`, `showcase-dialog.png` with Edda streaming a reply, `showcase-inventory.png` with seeded `gold_coin x12 + health_potion x3 + iron_ore x5` stacks and `KeyI` open, `showcase-night-lanterns.png` at `?dayNight=0.5` so the 4 plaza/doorway PointLights ramp full). Each scene reloads the page with its own URL params so the `?dayNight=` override applies cleanly. README rewritten: status table ticks P6.1-P6.11, new `## Gameplay` section (move/talk/quests/inventory/save/day-night/audio/debug), new `## Screenshots` 2×2 grid with captions, Stack updated to 64×3 grid + audio + persistence + full validation footprint. Build green 508.43 kB; `status.json` stays `exploring` per plan (graduation flip is P8.8).
iter 39 | done | P7.4 settings menu — `src/ui/settings.ts` `bindSettings({dayNight})` opens a modal with master volume slider (mirrors HUD slider both ways, calls `setMasterVolume` → persists `holtwick-voxel:audio:volume`), day-length slider 60-1800s (calls new `dayNight.setCycleSeconds` → persists `holtwick-voxel:dayLength`, preserves phase across mutation), and reset-save button (`window.confirm` → `clearSave()` + `window.location.reload()`). `world/dayNight.ts` `cycleSeconds` now mutable + restored from `DAY_LENGTH_KEY` at construction (clamped 60-1800), exposes `setCycleSeconds`/`getCycleSeconds`. `index.html` adds `#settings-gear` 36x36 amber-bordered button at top:14/right:16 (z-index 6) and `#settings-backdrop` modal (z-index 120 above inventory 110/dialog 100) with two ranges + reset + live value labels; HUD shifted to top:60 so the gear sits cleanly above it. `main.ts` calls `bindSettings({dayNight})` after `bindInventory()`. `validate-visual.mjs` P7.4 block clicks gear, asserts modal visible + 2 sliders + reset button, dispatches `input` on day-length=600 and asserts label reads "600s", captures `iter-${ITER}-settings.png`, Esc closes. Build green 517.49 kB (+2.42 kB vs iter 38).
iter 40 | done | P7.5 NPC idle barks — `src/ui/npcBark.ts` `updateNpcBarks({npcs, playerPos, camera, canvas, dialogNpcId, now})` runs per-frame: for each NPC inside an 8-voxel ring around the player and NOT currently in dialog, schedules a bark at random 15-30s intervals (per-NPC state map keyed by id), picks a random string from `def.barks_idle`, spawns a `.npc-bark` div under `#npc-bark-layer`, and each frame projects `mesh.position + (0,1.6,0)` through `camera.project` → `getBoundingClientRect()`-relative XY, applying `translate(x,y) translate(-50%,-100%)` so the bubble anchors above the head. Lifecycle = 200ms fade-in + 3500ms hold + 300ms fade-out = 4000ms total (TOTAL_MS); projection.z>1 (behind camera) hides the bubble for the frame. Test mode (`setBarkTestMode(true)` when `?test=1`) drops the interval to 150-400ms so the visual gate doesn't sit on a 30s wall-clock wait. `dialog.ts` exports new `getCurrentDialogNpcId()` returning the active NPC id only while the backdrop is shown; main.ts passes it each frame so the talking NPC stops barking mid-conversation. `index.html` adds `#npc-bark-layer` (position:fixed inset:0, pointer-events:none, z-index 50) + `.npc-bark` styling (amber-bordered dark pill, 220px max-width, monospace 12px) just before `#pickup-toast`. `main.ts` destructures `canvas` out of `bootstrapScene()` (renamed to `gameCanvas` to avoid the `#minimap` shadow), builds `barkNpcs: BarkNpc[]` once from `interactables`, and `__voxelTest__` gains `forceBark(id)` (bypasses the scheduler — fires immediately), `getBarkCount()` (live count of active barks), and `getBarkTexts()` (reads `.npc-bark.textContent` array) for the gate. `validate-visual.mjs` P7.5 block: warps player to `eddaPos + (0,1)`, calls `forceBark('edda')`, polls for `.npc-bark` count>=1 (timeout 3s), asserts `#npc-bark-layer` exists + bubble text is one of Edda's 6 `barks_idle` entries (inlined for the gate so the script stays vanilla mjs), captures `iter-${ITER}-bark.png`. Build green 519.84 kB (+2.35 kB vs iter 39).
iter 41 | done | P7.6 quest 3 "Visit the Well" — `src/data/quests.ts` adds `well_visit` entry: giver `hilda` (well-keeper spawned at (46,28), two cells north of well centre (46,30) — naturally inside the "3-cell radius" trigger described in the plan since you have to stand on Hilda's spawn cell to talk to her), `trigger: { type: "talk_to", npc_id: "hilda" }`, `reward: { gold: 5 }`. No dialog/quests state-machine changes needed — the existing `questsGivenBy(npc.id)` in `dialog.ts` already discovers giver=hilda and renders the accept-row, and `onTalkTo(npc.id)` already auto-completes any in_progress `talk_to` quest whose `npc_id` matches the dialog NPC, so the giver-and-target-are-the-same-NPC shape Just Works: first open shows accept (not_started → no auto-complete), accept transitions to in_progress, close + re-open and `onTalkTo("hilda")` finds the in_progress talk_to whose npc_id is "hilda" → completes, +5 gold. Demonstrates non-Edda quest-givers per plan intent. `validate-visual.mjs` P7.6 block snapshots `goldBefore`, `openDialog("hilda")`, asserts `#dialog-quest-row.show` with `#dialog-quest-title` containing "well", clicks `#dialog-quest-accept`, asserts state=in_progress, Escape, re-opens Hilda's dialog, asserts state=complete + gold === goldBefore + 5, captures `iter-${ITER}-well.png`. Build green 520.08 kB (+0.24 kB vs iter 40's 519.84 kB).

iter 42 | done | P7.7 quest 4 "Coins for a Song" — item-reward path now wired. `src/data/quest.schema.ts` loosens `reward` to `{ gold?: number; items?: QuestItemReward[] }` (both optional, back-compat for the 3 existing gold-only entries; new `QuestItemReward` interface keeps the shape named). `src/game/quests.ts` `completeQuest` gates gold on `def.reward.gold` and iterates `def.reward.items` via the existing `addItem` from `./inventory` (respects per-item stack caps + emits the inventory event the HUD/modal already listen to, so the reward visibly lands without further wiring). `src/data/quests.ts` adds `bren_5_coins` — plan called for "Bren the bard" but the tavern cast has Finn the Bard and no Bren, so giver = `finn` (already gives `finn_iron_ore`; `questsGivenBy` returns both quests for finn, `renderQuestRow` picks the first not_started, so iron_ore-complete + bren_5_coins-not_started ordering lets Finn surface the new quest cleanly on his next dialog open). `trigger: { type: "collect", item_id: "gold_coin", count: 5 }`, `reward: { items: [{ item_id: "health_potion", count: 1 }] }`. `src/ui/dialog.ts` extracts `rewardSummary(def)` so the `[Quest complete: …]` line renders "received 10 gold" OR "received Health Potion" OR "received 10 gold + Health Potion" depending on the reward shape (falls back to "no reward" defensively). `validate-visual.mjs` P7.7 block snapshots potion + gold, `acceptQuest("bren_5_coins")`, `addItem("gold_coin", 5)` → asserts state=complete, potion === before+1, gold unchanged (locks the non-gold reward path so a future schema regression that re-awards gold gets caught), opens Finn dialog for `iter-${ITER}-coins.png` capture, Esc closes. Build green 520.74 kB (+0.66 kB vs iter 41 520.08 kB).

iter 44 | done | P8.1 decorative props — `src/world/props.ts` `computeProps(seed, grid, reservedCells, count=20)` walks a separate mulberry32 stream (seed XOR 0xb022 so adding/removing props doesn't shift voxel layout or foliage). For each of 5 building zones (tavern footprint 8×6 at (28,14), blacksmith 6×6 at (18,20), well 5×5 centred at (46,30), 2 stalls 3×2 at (20,36)+(24,36)) computes the 1-cell outer ring, filters to cells that are (a) grass VOXEL_FLOOR at y=0 — skips dirt roads, plaza stone, pond water, plank floors AND (b) have an empty column at y≥1 — skips walls, foliage, well-ring stones AND (c) aren't reserved. Per-zone targets sum to 20: tavern 6 + blacksmith 6 + well 4 + stall-0 2 + stall-1 2. Type per prop is 50/50 barrel/crate from the same RNG. `buildPropMesh(props)` returns a THREE.Group with up to 2 InstancedMeshes (`props:barrel` using the darker dirt texture as banded staves, scale 0.6×0.9×0.6 centred at y=1.45; `props:crate` using the orange plank texture, scale 0.7³ centred at y=1.35) — both shadow-castring + receiving, both use the shared 1×1×1 BoxGeometry + the same NearestFilter/sRGB/Repeat texture cache pattern voxelMesh + foliage use. `main.ts` reserves NPC + item + tree cells when calling `computeProps`, positions the mesh at the same gridOffset as the voxel mesh, exposes `__voxelTest__.getPropPositions()` returning per-prop cell+world coords + type, and updates the boot log to "${props.length} props spawned". `validate-visual.mjs` P8.1 block asserts (a) 20 props total, (b) cell-coords land inside the inflated bounding box of ≥2 of {tavern, blacksmith, well, stalls}, then warps the player near a tavern-adjacent prop and captures `iter-${ITER}-props.png`. Build green 523.79 kB (+3.05 kB vs iter 43's 520.74 kB).
iter 45 | done | P8.2 toast notification stack — `src/ui/toast.ts` exports `pushToast(text)` + `visibleToastCount/queuedToastCount`. State holds at most `MAX_VISIBLE=3` active `.toast` divs; further pushes wait in `queue: string[]` and spawn via `drain()` once an older toast finishes its `SHOW_MS=2500` hold + `FADE_MS=250` fade-out. `spawn` appends a `.toast` into `#toast-container`, forces a layout flush (`void el.offsetWidth`) so the CSS transition runs from the initial state, then adds `.show` to drive opacity 0→1 + `translateY(40px→0)` over 250ms. `beginDismiss` removes `.show`, schedules DOM removal `FADE_MS` later, then calls `drain()` so the queue advances exactly once per slot freed. `index.html` replaces the ad-hoc `#pickup-toast` div + CSS with `#toast-container` (bottom-center flex column, gap 8px, z-index 8, `pointer-events: none`) and `.toast`/`.toast.show` rules matching the spec (280px, dark bg, white text, monospace, amber border, shadow). `src/main.ts` imports `pushToast`, removes the old `showPickupToast`+`toastTimer` helper, and on successful `addItem` calls `pushToast("+N Item Name")`. `__voxelTest__` gains `toast(text)` for the gate. `scripts/validate-visual.mjs` P8.2 block pushes 4 toasts via the hook, asserts exactly 3 `.toast` elements with the first three texts (4th queued, not in DOM yet), waits up to 4.5s for the 4th to materialise after the first toast's full hold+fade cycle, asserts final `.toast` count is ≤3, captures `iter-${ITER}-toast.png`. Build green 524.25 kB (+0.46 kB vs iter 44's 523.79 kB).

iter 43 | done | P7.8 README v2 — `README.md` rewritten: status table ticked through P7.7 with new rows for the 4 building types (tavern + forge + well + 2 stalls), the 12-NPC tavern cast, walking patrols, proximity idle barks, 4 quests across talk-to + collect with gold + item rewards, minimap, settings modal, title splash, mobile joystick/interact, and the CI Playwright gate. Added a dedicated **Controls** table covering WASD/arrows + joystick + E/Interact + I (inventory, focus-gated) + backquote (FPS) + gear (settings) + volume slider + Esc. Added a **Roster** table mapping all 12 NPC IDs → names → roles → quest-giver annotations, sourced from `src/data/tavernCast.ts` + `src/data/quests.ts` (Edda gives Find Aldric; Finn gives Iron for the Forge + Coins for a Song; Hilda gives Visit the Well). Added a **Recap by the numbers** section (4 quests / 3 items / 4 building types / 12 NPCs / 30 trees / 4 lanterns / 2-min default cycle / 64×3×64 grid). Gameplay section expanded with the explicit 4-quest list + buildings paragraph + HUDs paragraph (gold + quest log top-right, minimap top-left). Stack section refreshed (12 NPCs not the prior "31 lifted from holtwick-tavern" — those NPCs are dungeon mobs and the tavern roster is hand-authored on top), validation bullet now lists the full CI flow exercised end-to-end. Develop-locally block adds `npm run test:dialog` + notes both gates run in CI (`.github/workflows/visual-validation.yml`) not locally per the 2026-05-17 Playwright-off-the-dev-machine policy. `status.json` intentionally stays at `exploring` — graduation deferred to P9.6 per the plan. Build green 520.74 kB (no source changes — README-only).
iter 47 | done | P8.4 animated water + chimney smoke — `src/world/waterAnim.ts` `WaterAnimator(worldMesh)` finds the `voxel:5` (VOXEL_WATER) InstancedMesh inside the voxel group, snapshots per-instance baseY into a Float32Array on construction, and on `update(t)` writes `baseY + sin((t + i*0.3) * 1.5) * 0.05` per instance with one decompose/compose pass and `instanceMatrix.needsUpdate = true`. `src/render/particles.ts` `SmokeEmitter(basePosition)` builds a 64×64 radial-gray CanvasTexture (sRGB) shared across 16 `THREE.Sprite`s with `transparent: true`, `depthWrite: false`. Initial spawn phases are staggered by `i * (LIFETIME/SPRITE_COUNT)` so the column reads as a continuous trail at t=0 instead of all 16 pulsing in lockstep. Each sprite's outward drift direction is fixed at construction (`cos(angle)*DRIFT`, `sin(angle)*DRIFT` for `angle = i/16 * 2π`) so the trail spreads radially as it rises. `update(t)` recycles by computing `age = (t + phaseOffset) mod 4`, then `f = age/4`, writing position `(base.x + driftX*f, base.y + 2.5*f, base.z + driftZ*f)`, scale `0.5 + f*1.0` (uniform XY), and material opacity `0.7 * (1-f)`. `src/main.ts` constructs the emitter at world-coord `(gridOffset.x + 32.5, 5, gridOffset.z + 17.5)` (tavern roof centre per spec lifted onto worldMesh's gridOffset), `scene.add(smoke.group)`, binds `WaterAnimator(worldMesh)`, and both `smoke.update(t)` + `waterAnim.update(t)` fire from the RAF loop right after `worldItems` update (same `t = (now - bootMs)/1000` second clock). Test hook surfaces `getSmokeSpritePositions()` (16 entries with x/y/z/opacity) + `getWaterInstanceYs()` (per-instance Y array). `validate-visual.mjs` P8.4 block snaps both arrays, waits 1000ms, snaps again, asserts ≥1 water Y delta and ≥3 smoke sprite position deltas, captures `iter-${ITER}-smoke.png`. Build green 534.27 kB (+9.69 kB vs iter 46's 524.58 kB — smoke texture canvas + sprite material overhead).
iter 46 | done | P8.3 time-of-day HUD label — `src/ui/hud.ts` exports `setTimeOfDayLabel(phase)` that wraps the phase into [0,1) then maps via `Math.min(3, Math.floor(wrapped * 4))` to one of `["Morning", "Noon", "Dusk", "Night"]` so the spec bucket boundaries (0-0.25 / 0.25-0.5 / 0.5-0.75 / 0.75-1) are exact; the helper skips DOM writes when the new label matches the current `textContent`. `index.html` adds `<div id="hud-time">Morning</div>` directly below the minimap (top: 174px, width 150px to mirror the minimap, amber border + monospace caps, pointer-events: none, z-index 5). `src/main.ts` imports `setTimeOfDayLabel`, fires it once on init from `dayNight.currentPhase` and again every `TIME_LABEL_RENDER_EVERY=30` frames inside `frame()` next to the minimap tick; the test hook `setDayNightPhase` also calls it directly so the gate doesn't have to wait 30 frames after scrubbing the phase. `scripts/validate-visual.mjs` P8.3 block walks all 4 buckets (0.1→Morning, 0.4→Noon, 0.6→Dusk, 0.85→Night) via the test hook and asserts each label round-trips through `#hud-time`. Build green 524.58 kB (+0.33 kB vs iter 45's 524.25 kB).
iter 48 | done | P8.5 tavern sign + lamp posts — `src/world/decorations.ts` exports `buildTavernSign(gridOffset)` + `buildLampPosts(gridOffset)` + `updateLampPosts(lamps, phase)` + `lampIntensityForPhase(phase)`. Sign is a 1.5×0.6 `PlaneGeometry` with a 256×102 canvas texture (deterministic wood-grain horizontal lines + dark brown border + "The Holtwick Tavern" in cream serif), positioned at world (gridOffset.x+32.5, 3, gridOffset.z+20.05) so it hangs a hair south of the south wall outer face (TAVERN_ORIGIN_Z + TAVERN_INTERIOR_DEPTH + 2 = 20) and faces south by default since `PlaneGeometry`s normal is +Z and +Z is south here. Lamp posts are 4 `InstancedMesh` cubes (shared 1×4×1 `BoxGeometry`, dark `0x2a2520` `MeshStandardMaterial`) at cells (29,4)/(29,12)/(29,44)/(29,52) — west edge of the N/S road (cx=32 PATH_HALF=3 → road x=29..35), 8-voxel spacing, z values picked to avoid the tavern footprint (z=14..19) and the plaza ring (z=22..41) so no lamp lands inside a building. Each lamp gets a `PointLight(0xfff0a0, 0, 6)` at world y=4.8 (post top minus 0.2 so the bulb reads as glowing from the lantern); intensity is driven by `lampIntensityForPhase` — same `-cos(phase·2π) + 0.3` curve the tavern/plaza lanterns use, capped at `LAMP_MAX_INTENSITY=1.2` so the cluster ignites at dusk and peaks at midnight in lockstep with the existing lanterns. `src/main.ts` mounts the sign + instanced post mesh + 4 PointLights, calls `updateLampPosts(lampPosts, dayNight.currentPhase)` once at boot and once per RAF tick right after `updateLanterns`; the `?test=1` `setDayNightPhase` hook now drives both update functions so the gate does not have to wait for animation. New test hooks: `getLampIntensities()` returns 4 `{label, intensity}` entries; `hasTavernSign()` returns true when the mesh is parented to scene. `scripts/validate-visual.mjs` adds a P8.5 block (just before the runtime-errors check) that warps to phase 0.5 (midnight), asserts `hasTavernSign` is true + all 4 lamps have intensity > 0 + captures `iter-${ITER}-decorations.png`, then restores phase 0 so subsequent asserts dont see the lights. Build green 536.35 kB (+2.08 kB vs iter 47s 534.27 kB — sign canvas texture + shared box geometry + 4-instance matrix array).
